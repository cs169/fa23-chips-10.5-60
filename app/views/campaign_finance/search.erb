<div class="container">
    <div class="row">
        <div class="col text-center">
            <div class="p-4 mb-3 bg-light rounded">
                <h1 class="fst-italic">
                    <% @campaign_finances.each do |candidate| %>
                        </h1>
                        <%= candidate.name %>
                        </h1>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="container">
    <div class="col text-center">
        <%= link_to 'Back', :back, class: 'btn btn-secondary' %>
    </div>
</div>
